# 2026-06-05

## 双产物目录方案

- 根据新的实现目标，明确最终对外编译产物只保留 `minpatricia` 和 `minweight_store` 两个 library。
- 更新 `design/cpp_repository_layout.md`：
  - `src/minpatricia/` 生成独立 `minpatricia` 产物，可被外部项目单独使用。
  - `src/minweight_store/` 生成 `minweight_store` 产物，并 `PUBLIC` 链接 `minpatricia`。
  - `runtime` 不再作为第三个最终产物，而是收敛到 `minweight_store` 的 public `Env` 边界和 internal runtime adapter 中。
  - 后续应把当前初始骨架中的 `src/storage/` 和 `src/runtime/` 调整到 `src/minweight_store/` 内。
- 更新 `design/index.md` 中的目录设计说明。
- 按双产物方案调整 CMake 骨架：
  - `src/minpatricia` target 从 `minweight_minpatricia` 改为 `minpatricia` / `minpatricia::minpatricia`。
  - 新增 `src/minweight_store` target：`minweight_store` / `minweight_store::minweight_store`。
  - 移除旧的 `src/runtime` 和 `src/storage` 骨架入口。
  - 顶层 `src/CMakeLists.txt` 改为只添加 `minpatricia` 和 `minweight_store`。

## minpatricia 研究与计划

- 下载并研究 Go 版 `github.com/JimChengLin/minpatricia`：
  - 版本：`v0.0.0-20260531035544-2d1ddf70ce81`
  - commit：`2d1ddf70ce818228302912b81220b634218176b4`
- 读取核心实现文件：`types.go`、`index.go`、`node_store.go`、`record_store.go`、`diff.go`、`route.go`、`build.go`、`split.go`、`insert.go`、`delete.go`、`merge.go`、`iterator.go`、`retarget.go`、`index_test.go`、`index_fuzz_test.go`、`bench_test.go`。
- 新增 `design/minpatricia_port_plan.md`，记录：
  - Go 实现要点。
  - C++ API 和 `NodePage` layout 方案。
  - 分阶段翻译计划。
  - 正确性测试、model test、golden trace 计划。
  - Go 版性能基线和 C++ 性能测试计划。
- 更新 `design/index.md`，加入 `design/minpatricia_port_plan.md`。
- 根据用户确认，更新 `design/minpatricia_port_plan.md` 和 `trackers/todos.md`：
  - `minpatricia` 不做功能降级版或 MVP。
  - 以 C++20 全功能翻译和性能优化作为唯一完成标准。
  - 默认性能路径使用 concepts/templates，避免 hot path virtual dispatch。
  - benchmark 从一开始就是交付内容，目标是不慢于 Go，优先快于 Go。

## 验证

- `GOPATH=/data00/home/kuankuan.guo/minweight_store_cpp/.runtime/gopath GOMODCACHE=/data00/home/kuankuan.guo/minweight_store_cpp/.runtime/gopath/pkg/mod GOCACHE=/data00/home/kuankuan.guo/minweight_store_cpp/.runtime/go-build go test ./...` 在 Go 版 `minpatricia` 源码目录通过。
- Go 版 quick benchmark 已完成，结果记录在 `design/minpatricia_port_plan.md`。
- `cmake -S . -B build -DMINWEIGHT_BUILD_TESTS=ON` 成功。
- `cmake --build build` 成功。
- `ctest --test-dir build --output-on-failure` 成功执行。

## minpatricia C++20 实现进展

- 已实现 `src/minpatricia/include/minpatricia/`：
  - `status.h`：`StatusCode`、`Status`、`Result<T>`。
  - `byte_view.h`：`std::span<const std::byte>` key view 与 string/span helper。
  - `position.h`：`Position`、`kNodeSize`、`kMaxNodeReps`、`kChildTag`、`kMaxKeySize`。
  - `node_page.h`：`Rep`、`Route`、`NodePage`、route lookup、incremental route insert/delete、routeDiffs、layout static_assert。
  - `diff.h`：`CompareKeys`、`CompareAndDiffBit`、`FindDiffBit`、`GetDiffBit` 对齐 Go prefix/terminator diff 语义。
  - `record_store.h`：`RecordStoreLike` concept、`RecordStoreFunc`、`HeapRecordStore<V>`。
  - `node_store.h`：`NodeStoreLike` concept、`HeapNodeStore`。
  - `index.h`：模板化 `Index<RecordStore, NodeStore>`，覆盖 Go 版 `Probe/Get/Put/Delete/Retarget/Visit/Ascend/Descend/range/seek/OpenWithNodes/LiveNodes`，并移植 build/split/promoteSibling/merge/compressRoot 逻辑。
  - `minpatricia.h`：公开 umbrella header，提供 `NewWithRecords` owned-node wrapper 和 `NewHeap<V>` wrapper。
- 已新增 `src/minpatricia/minpatricia.cc` 作为静态库锚点。
- 已新增 `src/minpatricia/minpatricia_test.cc`，覆盖：
  - node layout、route/diff/prefix 语义。
  - heap record/node store 复用。
  - owned constructor、`OpenWithNodes`、non-zero root NodeStore。
  - Put/Get/Probe/Delete/Visit/Retarget。
  - Ascend/Descend/range/seek/early stop。
  - 2500 key 多节点 split/replace/delete 模型对照。
  - 替换和 Retarget child boundary 后的父节点 boundary cache 传播。
  - 3000 步确定性 map model test，覆盖 put/delete/get/seek/range。
  - key-too-large、tagged position、corrupt route 等错误路径。
  - `src/minpatricia/testdata/` golden trace：diff、route、seek、small ops，并由 C++ 测试读取校验。
  - Go fuzz seed 回放：alpha/bravo、empty/NUL-byte key、`MaxNodeReps + 1` split seed。
  - delete-all route consistency、100K delete-heavy lookup consistency。
  - 递归 route validation：每个 `NodePage::RouteDiffs()` 与按 record key 重新计算出的 diff 序列一致。
- 新增 `design/minpatricia_go_ut_coverage.md`，逐项记录 Go `index_test.go`、`coverage_test.go`、`index_fuzz_test.go`、`bench_test.go` 与 C++ 测试/benchmark 的对应关系。
- 已新增 `src/minpatricia/minpatricia_bench.cc`，当前轻量覆盖：
  - Get/1K, Get/10K
  - PutReplace/1K, PutReplace/10K
  - PutInsert/1K, PutInsert/10K
  - Seek/1K, Seek/10K
  - ReverseSeek/1K, ReverseSeek/10K
  - VisitFullSetOrdered/1K, VisitFullSetOrdered/10K
  - VisitFullSetReverse/1K, VisitFullSetReverse/10K
  - Footprint/1K, Footprint/10K
  - DeleteHeavy/10K
  - DeleteHeavyFootprint/10K
  - 设置 `MINPATRICIA_BENCH_LARGE=1` 后覆盖 100K 和 DeleteHeavy/100K。
- 已新增 Go-generated benchmark fixture：
  - `src/minpatricia/testdata/gen_bench_fixtures.go`
  - `bench_keys_1K.tsv`
  - `bench_keys_10K.tsv`
  - `bench_keys_100K.tsv`

## 当前性能结果

当前 C++ benchmark 命令：

```bash
cmake --build build --target minpatricia_bench -j2
./build/src/minpatricia/minpatricia_bench
```

结果：

归档位置：`.runtime/benchmarks/2026-06-05/cpp_minpatricia_bench.txt`。

```text
Get/1K              65 ns/op
Get/10K             119 ns/op
PutReplace/1K       84 ns/op
PutReplace/10K      136 ns/op
PutInsert/1K        352 ns/op
PutInsert/10K       492 ns/op
Seek/1K             103 ns/op
Seek/10K            152 ns/op
ReverseSeek/1K      107 ns/op
ReverseSeek/10K     157 ns/op
VisitFullSetOrdered/1K 4 ns/item
VisitFullSetOrdered/10K 4 ns/item
VisitFullSetReverse/1K 4 ns/item
VisitFullSetReverse/10K 4 ns/item
Footprint/1K        16.384 node_B/key, 4 nodes
Footprint/10K       20.48 node_B/key, 50 nodes
DeleteHeavy/10K     110 ns/op
DeleteHeavyFootprint/10K 25.7941 node_B/live_key, 49 nodes
```

large 归档位置：`.runtime/benchmarks/2026-06-05/cpp_minpatricia_bench_large.txt`。

```text
Get/100K            162 ns/op
PutReplace/100K     177 ns/op
PutInsert/100K      611 ns/op
Seek/100K           193 ns/op
ReverseSeek/100K    197 ns/op
VisitFullSetOrdered/100K 7 ns/item
VisitFullSetReverse/100K 6 ns/item
Footprint/100K      21.0944 node_B/key, 515 nodes
DeleteHeavy/100K    140 ns/op
DeleteHeavyFootprint/100K 30.7861 node_B/live_key, 508 nodes
```

同机重新跑 Go 版 minpatricia-only benchmark：

归档位置：`.runtime/benchmarks/2026-06-05/go_minpatricia_bench_large.txt`。

```text
Get/1K              78.23 ns/op
Get/10K             134.4 ns/op
Get/100K            184.7 ns/op
PutReplace/1K       91.46 ns/op
PutReplace/10K      146.5 ns/op
PutReplace/100K     195.9 ns/op
PutInsert/1K        401.9 ns/op
PutInsert/10K       568.1 ns/op
PutInsert/100K      697.3 ns/op
VisitFullSetOrdered/1K 11105 ns/op total
VisitFullSetOrdered/10K 111977 ns/op total
VisitFullSetOrdered/100K 1656995 ns/op total
VisitFullSetReverse/1K 10484 ns/op total
VisitFullSetReverse/10K 105576 ns/op total
VisitFullSetReverse/100K 1604944 ns/op total
Seek/1K             113.2 ns/op
Seek/10K            162.2 ns/op
Seek/100K           213.4 ns/op
ReverseSeek/1K      115.0 ns/op
ReverseSeek/10K     164.9 ns/op
ReverseSeek/100K    215.4 ns/op
DeleteHeavy/10K     126.8 ns/op
DeleteHeavy/100K    155.4 ns/op
Footprint/1K        16.38 node_B/key, 4 nodes/op
Footprint/10K       20.48 node_B/key, 50 nodes/op
Footprint/100K      21.09 node_B/key, 515 nodes/op
DeleteHeavyFootprint/10K 25.79 node_B/live_key, 49 nodes/op
DeleteHeavyFootprint/100K 30.79 node_B/live_key, 508 nodes/op
```

说明：

- C++ benchmark 现在读取 Go-generated fixed fixtures；1K/10K/100K keyset 与 Go benchmark 的 `newBenchData(n)` 同源。
- C++ benchmark 使用 benchmark-only borrowed `RecordStore`，用于测 `minpatricia` index 本体；库内默认 `HeapRecordStore` 仍保持拥有式 key copy。
- 热路径未引入 virtual dispatch；默认 `Index<RecordStore, NodeStore>` 通过 C++20 concepts/templates 静态分发。
- TODO-1 已完成并移动到 `trackers/resolved.md`。

## minpatricia 稳定设计文档

- 根据完成后的 C++20 实现，新增 `design/minpatricia.md`。
- 文档覆盖：
  - 模块职责与非职责。
  - public API、`Probe`/`Get`/`Retarget` 语义差异。
  - `RecordStore`、`NodeStore` 接入方式。
  - `Position`、`Rep`、`NodePage`、`Route` 数据结构。
  - diff bit、Cartesian route、Put、Delete、Retarget、Iterator/seek 算法原理。
  - 正确性不变量、性能设计、benchmark 结果和测试覆盖。
  - 与 `minweight_store` 的 WAL、record、mmap、runtime 集成边界。
- 更新 `design/index.md`，加入 `design/minpatricia.md` 作为稳定设计入口。

## README 仓库说明更新

- 更新 `README.md`：
  - 明确仓库来源于原 Go/golang 版本 `minweight_store`，当前使用 C++20 重新构建。
  - 说明最终对外保留 `minpatricia` 和 `minweight_store` 两个 library。
  - 补充 `minpatricia` 的核心用途、API 边界、纯索引职责、性能对比和最小使用示例。
  - 说明 `minweight_store` 的计划用途和场景，并标注当前尚未完成 Go 到 C++ 的翻译。
