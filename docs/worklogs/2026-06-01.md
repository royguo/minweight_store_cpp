# 2026-06-01

## 仓库重建

- 从原 `main` 创建 `golang` 分支并推送到 `origin/golang`，作为 Go 版本的持久备份。
- 在本地创建 `golang_code/` 快照目录，包含重构前的 Go 工作树内容；该目录已加入 `.gitignore`，不进入代码追踪。
- 使用 orphan commit 重建 `main`，移除原 Go 代码和历史文件。
- 复制 `/home/kuankuan.guo/KyteStore/docs/agents_read.md` 到 `docs/agents_read.md`，作为本项目文档协作规则。
- 创建 CMake 骨架：`CMakeLists.txt`、`src/CMakeLists.txt`、`src/minpatricia/`、`src/runtime/`、`src/storage/`。
- 创建文档骨架：`docs/design/`、`docs/discuss/`、`docs/trackers/`、`docs/worklogs/`。

## 设计与任务

- 新增 `design/cpp_repository_layout.md`，明确 CMake、`third/`、`src/`、测试同目录和 `minpatricia` 合仓规则。
- 新增 `design/cpp_porting_plan.md`，记录 C++ 结构化移植阶段计划和高风险点。
- 新增 `trackers/todos.md`，将首批 C++ 移植任务拆分为 TODO-1 到 TODO-9。

## 验证

- `cmake -S . -B build -DMINWEIGHT_BUILD_TESTS=ON` 成功。
- `cmake --build build` 成功。
- `ctest --test-dir build --output-on-failure` 成功执行，当前尚无测试用例。
- `git push --force origin main` 成功，远端 `main` 更新到 C++ 初始骨架。
